- You can use `?` to do something like the if statement

```js
const VALUE1 = true;
const VALUE2 = false;

// If the condition is met, then VALUE1 is returned. Otherwise, VALUE2.
let var1 = condition ? VALUE1 : VALUE2;
```